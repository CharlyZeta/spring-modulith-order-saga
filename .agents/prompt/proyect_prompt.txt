CONTEXTO:
Soy un desarrollador Backend Senior trabajando en un sistema de agregación de empleos en Java 21 + Spring Boot. Mi sistema utiliza un modelo de datos normalizado llamado JobOpportunity.

REGLAS DE TRABAJO:

El código generado debe seguir estándares de Clean Code y SOLID.

Uso de Record de Java 21 para DTOs.

Las peticiones deben usar WebClient de Spring.

La normalización debe manejar: conversión de monedas (a USD o ARS), mapeo de Seniority (Junior, Ssr, Sr) y extracción de tags técnicos.

TU TAREA:
Analiza la siguiente documentación/JSON de API que te proporcionaré a continuación.

Identifica los Endpoints clave: Búsqueda, detalle y filtros (salario, ubicación).

Crea el DTO de respuesta específico: Genera los Java Records que mapeen el JSON original de esta API.

Genera el Mapper: Crea un componente @Component que transforme ese DTO específico a mi JobOpportunity global.

Identifica Limitaciones: Indica si requiere API Key, Rate Limiting y si el campo de salario es numérico o texto libre.

FORMATO DE SALIDA:
Devuelve el código Java necesario para integrar esta nueva fuente en mi paquete com.myapp.integration.sources.[NombreAPI].